spring:
    main:
        allow-circular-references: true
    profiles:
        application:
            name: damri
        datasource:
            url: jdbc:postgresql://10.54.138.2:5432/damri
            username: postgres
            password: postgres
            driver-class-name: org.postgresql.Driver
    jpa:    
        properties:
            hibernate:
                jdbc:
                    lab:
                        non_contextual_creation: true
                dialect: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: none